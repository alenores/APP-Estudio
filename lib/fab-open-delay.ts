/** Pausa entre el active del botón FAB y abrir el sheet (feedback táctil). */
export const FAB_OPEN_DELAY_MS = 120;

/** Mantener apretada una card hijo para menú contextual (seguimiento / concepto). */
export const LONG_PRESS_MS = 480;
